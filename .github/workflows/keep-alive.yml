name: Keep Dashboard Alive

on:
  schedule:
    - cron: '*/5 * * * *'  # every 10 minutes
  workflow_dispatch:         # allows manual trigger from GitHub UI

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Dashboard
        run: |
          curl --fail https://ai-pipeline-leak-detection.onrender.com
          echo "Pinged dashboard at $(date)"