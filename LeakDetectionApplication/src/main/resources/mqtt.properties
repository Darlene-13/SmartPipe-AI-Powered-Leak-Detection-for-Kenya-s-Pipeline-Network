# HiveMQ Cloud Broker
broker=ssl://e99d5c54b1484faca109d2adb21afc8d.s1.eu.hivemq.cloud:8883

# Client
clientId=JavaSpringClient

# Credentials ? use what you just created
username=leakdetection
password=#Nasimiyu12345

# Inbound subscribe topics
subscribe.topics=pipeline/devices/register,\
                 pipeline/sensors/+/node,\
                 pipeline/sensors/+/status,\
                 pipeline/sensors/+/diagnostic

# Outbound publish base paths
publish.topic.config=pipeline/devices/%s/config
publish.topic.alerts=pipeline/alerts/%s
publish.topic.commands=pipeline/commands/%s

# Connection settings
cleanSession=true
connectionTimeout=30
keepAliveInterval=60